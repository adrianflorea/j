NB. can apply to nouns or verbs
NB. has only a left argument (it's monadic and comes after the verb they modify as in English)
NB. has a verb as its result so an adverb can "modify" a verb

NB. Reflex ~

NB. AND OR tables
] (*./~ ; +./~) 0 1

NB. Insert /
NB. Mean (monadic fork) of the first 10 positive numbers
] (+/ % #) >:i.10

NB. Prefix \
] </\ 0 0 0 1 0 1 1 0

NB. Suffix \.
] <\. i.4
